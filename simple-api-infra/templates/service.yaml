apiVersion: v1
kind: Service
metadata:
  name: {{ include "simple-api-infra.fullname" . }}
  labels:
    {{- include "simple-api-infra.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simple-api-infra.selectorLabels" . | nindent 4 }}
