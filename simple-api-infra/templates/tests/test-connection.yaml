apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "simple-api-infra.fullname" . }}-test-connection"
  labels:
    {{- include "simple-api-infra.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "simple-api-infra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
