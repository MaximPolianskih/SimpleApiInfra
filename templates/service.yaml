apiVersion: v1
kind: Service
metadata:
  name: {{ include "SimpleApiInfra.fullname" . }}
  labels:
    {{- include "SimpleApiInfra.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "SimpleApiInfra.selectorLabels" . | nindent 4 }}
